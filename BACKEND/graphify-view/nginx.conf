server {
    listen 80;
    server_name _;
    root /usr/share/nginx/html;
    charset utf-8;

    location = / {
        return 302 /graph-view.html;
    }

    location / {
        try_files $uri $uri/ =404;
    }
}
