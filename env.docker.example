# Copy to .env next to docker-compose.yaml (never commit .env).
POSTGRES_PASSWORD=replace_with_long_random_password
JWT_SECRET=replace_with_at_least_32_random_characters_here
REDIS_PASSWORD=
HTTP_PORT=80
